services:
  - type: web
    name: iris-ml-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && python -m ml.train
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
